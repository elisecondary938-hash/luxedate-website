---
const { class: className = '' } = Astro.props;
---
<a href="/" class={`inline-flex items-center gap-2 ${className}`} aria-label="LuxeDate">
  <span class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-white/10 bg-white/5">
    <span class="h-2 w-2 rounded-full bg-champagne-500" />
  </span>
  <span class="text-sm font-semibold tracking-wide">LuxeDate</span>
</a>
