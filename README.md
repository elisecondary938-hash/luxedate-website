# LuxeDate Marketing Site (Astro + Tailwind)

Static marketing site pages:
- `/` home
- `/pricing`
- `/waitlist`
- `/faq`
- `/privacy`
- `/terms`

## Notes
- This project includes Tailwind config and Astro pages/components.
- The waitlist form is a **static placeholder** (no backend wiring here).

## Recommended deployment path (later)
- Vercel, Netlify, or Cloudflare Pages
- Connect repo, set build command `npm run build`, output `dist/`
- Add real domain + analytics + form handler (or a serverless endpoint) when ready
